<%- include('../partials/header') -%>
<h1><%= foodItem.meals[0].strMeal %></h1>

<% foodItem.meals.forEach(function(meal) { %>

<img src="<%= meal.strMealThumb %>" alt="Food Picture" />
<br /><br />

<% }); %>

<h1><%= foodItem.meals[0].strMeal %></h1>
<ul>
  <% for (let i = 1; i <= 20; i++) { %> 
    <% if
  (foodItem.meals[0][`strIngredient${i}`]) { %>
  <li><%= foodItem.meals[0][`strIngredient${i}`] %></li>
  <% } %> 
  <% } %>
</ul>

<%- include('../partials/footer') -%>
